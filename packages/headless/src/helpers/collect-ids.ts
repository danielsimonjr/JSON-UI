// Moved to @json-ui/core — this file is a thin re-export for backward
// compatibility with any existing `import {collectFieldIds} from "./helpers/collect-ids"`
// calls inside the headless package. New code should import from
// `@json-ui/core` directly.
export { collectFieldIds } from "@json-ui/core";
